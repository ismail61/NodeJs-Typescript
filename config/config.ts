/* 
const {  PORT, DB_URL, TOKEN_KEY } = process.env
//console.log(process.env)
const config = {
    app: {
        port: PORT | 4001
    },
    db: {
        url: DB_URL
    },
    /* cloudinary: {
        name: CLOUDINARY_NAME,
        api: CLOUDINARY_API_KEY,
        secret: CLOUDINARY_API_SECRET
    }, */
    jwtToken: {
        key: TOKEN_KEY
    },
};


export default config; */